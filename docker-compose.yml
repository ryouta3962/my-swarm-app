version: '3.8'

services:
  my-web:
    # ここがポイント: 'latest' や特定のバージョンを明記しておくと管理しやすくなります
    image: nginx:latest
    ports:
      - "8080:80"
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
