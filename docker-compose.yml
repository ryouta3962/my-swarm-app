version: '3.8'

services:
  my-web:
    # 🌟ここが重要！Docker Hubのあなたのユーザー名に変更してください
    image: ryoute/my-swarm-app:latest
    ports:
      - "8080:80"
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure